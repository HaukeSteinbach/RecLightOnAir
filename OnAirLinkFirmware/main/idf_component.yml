## Managed dependencies (fetched by the IDF component manager on build).
dependencies:
  # Espressif's ESP32-patched asio (lwip shims). Ableton Link's networking
  # rides on it; vanilla asio will not compile for the ESP32.
  espressif/asio: "*"
  idf:
    version: ">=5.0"
